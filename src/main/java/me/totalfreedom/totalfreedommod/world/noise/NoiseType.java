package me.totalfreedom.totalfreedommod.world.noise;

/** Which of Bukkit's generators backs a field. Both live in org.bukkit.util.noise, so no new deps. */
public enum NoiseType
{
    PERLIN,
    SIMPLEX
}
